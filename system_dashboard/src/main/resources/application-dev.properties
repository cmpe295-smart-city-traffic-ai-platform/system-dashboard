spring.datasource.url=${POSTGRES_HOST:jdbc:postgresql://localhost:5432/smart_city_traffic}
spring.datasource.username=${POSTGRES_USERNAME:iot_station}
spring.datasource.password=${POSTGRES_PASSWORD:iot_station}
#mongoDB--needs to be updated
spring.data.mongodb.database=${MONGO_DATABASE}
spring.data.mongodb.uri=mongodb+srv://${MONGO_USER}:${MONGO_PASSWORD}@${MONGO_CLUSTER}?${MONG0_POOL_SIZE}

#other
spring.devtools.livereload.enabled=true
spring.jpa.hibernate.ddl-auto=update